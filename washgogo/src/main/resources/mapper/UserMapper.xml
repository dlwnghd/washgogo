<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.washgogo.mapper.UserMapper">
    <select id="getList" resultType="userVO">
        SELECT USER_NUMBER, USER_EMAIL, USER_PW, USER_NAME, USER_PHONENUM, USER_ADDRESS, USER_ENTRANCE_TYPE, USER_ENTRANCE_PW, USER_ENTRANCE_MESSAGE, USER_SERVICE_TYPE, USER_CARDNUM
        FROM TBL_USER
    </select>

    <!--회원 가입-->
    <insert id="insert">
        <selectKey keyProperty="userNumber" order="BEFORE" resultType="long">
            SELECT SEQ_USER.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO TBL_USER (USER_NUMBER, USER_EMAIL, USER_PW, USER_NAME, USER_PHONENUM)
        VALUES (#{userNumber}, #{userEmail}, #{userPw}, #{userName}, #{userPhonenum})
    </insert>

    <!--마이페이지-->
    <select id="selectUserInfo" resultType="userVO">
        SELECT USER_EMAIL, USER_PW, USER_NAME, USER_PHONENUM, USER_ADDRESS, USER_ENTRANCE_TYPE, USER_ENTRANCE_PW, USER_ENTRANCE_MESSAGE, USER_SERVICE_TYPE, USER_CARDNUM
        FROM TBL_USER;
    </select>

    <!--회원 정보 수정-->
    <update id="updateUserInfo">
        UPDATE TBL_USER
        SET USER_EMAIL = #{userEmail}, USER_PW = #{userPw}, USER_NAME = #{userName}, USER_PHONENUM = #{userPhonenum}, USER_ADDRESS = #{userAddress}, USER_ENTRANCE_TYPE = #{userEntranceType}, USER_ENTRANCE_PW = #{userEntrancePw}, USER_ENTRANCE_MESSAGE = #{userEntranceMessage}, USER_SERVICE_TYPE = #{userServiceType}, USER_CARDNUM = #{userCardNum}
        WHERE USER_NUMBER=#{userNumber}
    </update>

    <!--서비스 변경-->
    <update id="updateService">
        UPDATE TBL_USER
        SET USER_SERVICE_TYPE = #{userServiceType}
        WHERE USER_NUMBER=#{userNumber}
    </update>

    <!--주소 입력 및 수정-->
    <update id="updateAddress">
        UPDATE TBL_USER
        SET USER_ADDRESS = #{userAddress}, USER_ENTRANCE_TYPE = #{userEntranceType}, USER_ENTRANCE_PW = #{userEntrancePw}, USER_ENTRANCE_MESSAGE = #{userEntranceMessage}
        WHERE USER_NUMBER=#{userNumber}
    </update>

    <!--회원 탈퇴-->
    <delete id="delete">
        DELETE FROM TBL_USER
        WHERE USER_NUMBER = #{userNumber}
    </delete>

</mapper>