<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.washgogo.mapper.PriceMapper">
    <!--추가-->
    <insert id="insert">
        <selectKey keyProperty="priceNumber" order="BEFORE" resultType="long">
            SELECT SEQ_PRICE.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO TBL_PRICE (PRICE_NUMBER, ITEM, AMOUNT,PRICE,USER_NUMBER,POINT_USE)
        VALUES (#{priceNumber}, #{item}, #{amount},#{price},#{userNumber},#{pointUse})
    </insert>
</mapper>